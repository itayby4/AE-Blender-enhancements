[project]
name = "mcp-aftereffects"
version = "0.1.0"
description = "MCP server for Adobe After Effects"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]"
]

[project.scripts]
mcp-aftereffects = "mcp_aftereffects.server:main"

// ── Transitional barrel — source moved to @pipefx/brain-memory ──
export type {
  ProjectDTO,
  KnowledgeDTO,
  KnowledgeInsert,
  KnowledgeCategory,
  KnowledgeSource,
  SessionDTO,
  TaskDTO,
  TaskStep,
  TaskStatus,
  TaskEvent,
  TaskRow,
  AssembledContext,
} from '@pipefx/brain-memory';

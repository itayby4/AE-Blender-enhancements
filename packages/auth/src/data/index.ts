// Auth data access — stub.
//
// Structural placeholder for the data layer. The phase-03 package shape
// reserves data/ for typed queries and the auth_sessions schema (local-mode
// cache). Migration is deferred to a later behavioral phase; this file
// exists so the layer-tag + deep-import enforcement rules have a concrete
// target.
//
// Not exported from package.json — internal to @pipefx/auth.
export {};

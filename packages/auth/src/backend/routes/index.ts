// Auth HTTP routes — stub.
//
// Structural placeholder for the routes sub-layer. The phase-03 package shape
// reserves backend/routes/ for POST /auth/sign-in, POST /auth/sign-out, and
// GET /auth/session handlers. Migration of those endpoints from the monolithic
// apps/backend/src/main.ts request dispatcher is deferred to a later
// behavioral phase; this file exists so the layer-tag + deep-import
// enforcement rules have a concrete target.
//
// Not exported from package.json — internal to @pipefx/auth/backend.
export {};

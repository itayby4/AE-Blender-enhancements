// ── @pipefx/skills — root barrel ─────────────────────────────────────────
// The public surface exposes `./contracts` (frozen types + events + ports).
// Domain primitives, backend mount, UI, and authoring land in subpath
// exports as Phase 7 sub-phases progress.
export * from './contracts/index.js';
